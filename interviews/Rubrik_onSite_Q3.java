Q3
// Given a list of boxes with length, width and height, assuming you cannot rotate the boxes,
// (e.g. cannot make length as height, height as length)
// return the tallest tower height you can build with those boxes
// rules: the box on bottom MUST have a larger or equal width/length of the one on top of it

failed to get the best solution at first, after the interviewers hint, made it